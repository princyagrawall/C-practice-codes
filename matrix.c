#include <stdio.h>
int main(void){
    int rows;
    int columns;
    printf("write the number of rows:");
    scanf("%d",rows);
    printf("write the number of columns:");
    scanf("%d",columns);

    int classicMat[rows][columns];
    for (int i=0;i<rows;i++);
    {
        mat[i]=malloc{columns *sizeof(int*)};
    }


}